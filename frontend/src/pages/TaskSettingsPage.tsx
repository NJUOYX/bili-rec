/**
 * 任务级设置页占位（§8.1 null 回退，随 M31 设置模块实现）。
 */
import { PlaceholderPage } from './PlaceholderPage'

export function TaskSettingsPage() {
  return <PlaceholderPage title="任务设置" hint="任务级设置实现中（M31）" />
}
